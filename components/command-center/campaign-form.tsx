import { CheckboxGroup, Field, Select, SubmitButton, TextArea } from "./form-fields";

const objectiveTypes = ["USER_ACQUISITION", "PARTICIPATION", "RETENTION", "BRAND_AWARENESS", "CREATOR_ACTIVATION", "MEDIA_EARNED", "PARTNERSHIP", "INDUSTRY_OUTREACH", "PRODUCT_VALIDATION", "OTHER"];
type CampaignValue = { name: string; programId: string | null; description: string | null; objective: string | null; objectiveType: string; primaryAudience: string | null; primaryCta: string | null; coreMessage: string | null; sport: string | null; season: string | null; notes: string | null; successDefinition: string | null; kpis: string[]; startsAt: Date | null; endsAt: Date | null; status: string; brands: { id: string }[]; markets: { id: string }[]; teams: { id: string }[] };

export function CampaignForm({ action, programs, brands, markets, teams, campaign }: { action: (formData: FormData) => void | Promise<void>; programs: { id: string; name: string }[]; brands: { id: string; name: string }[]; markets: { id: string; name: string }[]; teams: { id: string; name: string }[]; campaign?: CampaignValue }) {
  return <form action={action} className="space-y-5 rounded-2xl border border-white/8 bg-[#101214] p-6"><div className="grid gap-4 sm:grid-cols-2"><Field label="Campaign name" name="name" required defaultValue={campaign?.name} /><Select label="Growth program" name="programId" defaultValue={campaign?.programId || ""}><option value="">Unassigned</option>{programs.map((program) => <option key={program.id} value={program.id}>{program.name}</option>)}</Select></div><TextArea label="Business / growth objective" name="objective" rows={3} defaultValue={campaign?.objective || ""} /><div className="grid gap-4 sm:grid-cols-2"><Select label="Objective type" name="objectiveType" defaultValue={campaign?.objectiveType || "OTHER"}>{objectiveTypes.map((type) => <option key={type}>{type}</option>)}</Select><Select label="Status" name="status" defaultValue={campaign?.status || "PLANNING"}>{["PLANNING","ACTIVE","PAUSED","COMPLETE","ARCHIVED"].map((status) => <option key={status}>{status}</option>)}</Select></div><div className="grid gap-4 sm:grid-cols-2"><TextArea label="Primary audience" name="primaryAudience" rows={2} defaultValue={campaign?.primaryAudience || ""} /><TextArea label="Core message / positioning" name="coreMessage" rows={2} defaultValue={campaign?.coreMessage || ""} /></div><Field label="Primary CTA" name="primaryCta" defaultValue={campaign?.primaryCta || ""} /><TextArea label="Success definition" name="successDefinition" rows={2} defaultValue={campaign?.successDefinition || ""} /><TextArea label="KPIs (one per line)" name="kpis" rows={4} defaultValue={campaign?.kpis.join("\n") || ""} /><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Field label="Sport" name="sport" defaultValue={campaign?.sport || ""} /><Field label="Season" name="season" defaultValue={campaign?.season || ""} /><Field label="Starts" name="startsAt" type="date" defaultValue={campaign?.startsAt?.toISOString().slice(0,10) || ""} /><Field label="Ends" name="endsAt" type="date" defaultValue={campaign?.endsAt?.toISOString().slice(0,10) || ""} /></div><TextArea label="Description" name="description" rows={2} defaultValue={campaign?.description || ""} /><TextArea label="Operating notes" name="notes" rows={3} defaultValue={campaign?.notes || ""} /><div><p className="mb-2 text-xs text-[#747976]">Brands</p><CheckboxGroup name="brandIds" items={brands} selectedIds={campaign?.brands.map((item) => item.id)} /></div><div><p className="mb-2 text-xs text-[#747976]">Markets</p><CheckboxGroup name="marketIds" items={markets} selectedIds={campaign?.markets.map((item) => item.id)} /></div><div><p className="mb-2 text-xs text-[#747976]">Teams</p><CheckboxGroup name="teamIds" items={teams} selectedIds={campaign?.teams.map((item) => item.id)} /></div><SubmitButton>{campaign ? "Save Campaign" : "Create Campaign"}</SubmitButton></form>;
}
